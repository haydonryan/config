#!/bin/bash

git config --global user.email "haydon.ryan@gmail.com"
git config --global user.name "Haydon Ryan"
git config --global pull.rebase true
git config --global fetch.prune true
git config --global diff.colorMoved zebra
