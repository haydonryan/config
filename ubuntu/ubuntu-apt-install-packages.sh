#!/bin/bash

apt install -y git alpine-sdk curl vim go tmux ssh openssh nmap google-test cmake golang golang-golang-x-tools gnome-tweaks
apt install -y gimp inkscape krita

# Shotcut is a free video editor
