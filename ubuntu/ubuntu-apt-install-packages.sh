#!/bin/bash
sudo add-apt-repository ppa:neovim-ppa/stable # add neovim repo
apt install -y git alpine-sdk curl neovim go tmux ssh openssh nmap google-test cmake golang golang-golang-x-tools gnome-tweaks vlc
apt install -y gimp inkscape krita gddrescue nodejs

# Shotcut is a free video editor
