#!/bin/bash
sudo add-apt-repository ppa:neovim-ppa/stable # add neovim repo
apt install -y git alpine-sdk curl vim go tmux ssh openssh nmap google-test cmake golang golang-golang-x-tools gnome-tweaks vlc
apt install -y gimp inkscape krita gddrescue nodejs neovim

# Shotcut is a free video editor
